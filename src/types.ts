export type RootStackParamList = {
    Title: undefined;
    Game1: { mode: number };
    Game2: { mode: number };
    Game3: { mode: number };
    ModeSelect: undefined;
    Result: { finalScore: number; mode: number };
}